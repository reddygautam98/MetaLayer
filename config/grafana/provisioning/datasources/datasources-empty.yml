# =====================================================
# GRAFANA DATA SOURCES PROVISIONING - MINIMAL CONFIG
# Empty configuration to prevent startup issues
# Datasources can be added manually via Grafana UI
# =====================================================

apiVersion: 1

# Empty datasources list to prevent provisioning errors
datasources: []